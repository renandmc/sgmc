{% extends 'base.html.twig' %}
{% block body %}
    <h1>Novo departamento</h1>
    <a href="{{ path('departamentos_index') }}" class="btn btn-default">Voltar para lista</a>
    <hr>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-default" value="Salvar">
    {{ form_end(form) }}
{% endblock %}