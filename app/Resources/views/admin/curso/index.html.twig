{% extends 'admin/index.html.twig' %}
{% block main %}
    <h1>Cursos</h1>
    <a href="{{ path('admin_cursos_novo') }}" class="btn btn-primary">Novo</a>
    <hr>
    <table class="table table-responsive">
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Descrição</th>
            <th>Opções</th>
        </tr>
        {% for curso in cursos %}
            <tr>
                <td>{{ curso.id }}</td>
                <td>{{ curso.nome }}</td>
                <td>{{ curso.descricao }}</td>
                <td>
                    <a href="{{ path('admin_cursos_info', { 'id': curso.id }) }}" class="btn btn-default">Info</a>
                    <a href="{{ path('admin_cursos_editar', { 'id': curso.id }) }}" class="btn btn-warning">Editar</a>
                    <a href="{{ path('admin_cursos_excluir', { 'id': curso.id }) }}" class="btn btn-danger">Excluir</a>
                </td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}