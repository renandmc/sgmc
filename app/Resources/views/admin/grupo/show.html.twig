{% extends 'base.html.twig' %}

{% block body %}
    <h1>Grupo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grupo.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_grupos_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_grupos_edit', { 'id': grupo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
