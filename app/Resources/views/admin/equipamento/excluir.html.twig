{% extends 'admin/index.html.twig' %}
{% block main %}
    <h1>
        <a href="{{ path('admin_equipamentos_index') }}" class="btn btn-default">
            <span class="glyphicon glyphicon-arrow-left"></span>
        </a>
        Excluir equipamento
    </h1>
    <hr>
    <h3>ID: {{ equipamento.id }}</h3>
    <h3>Nome: {{ equipamento.nome }}</h3>
    <hr>
    <h2>Deseja excluir o equipamento?</h2>
    <form action="{{ path('admin_equipamentos_excluir', {'id': equipamento.id}) }}" method="post">
        <input type="submit" value="Sim" name="del" class="btn btn-danger">
        <input type="submit" value="Não" name="del" class="btn btn-default">
    </form>
{% endblock %}